import React from "react";
import styled from "@emotion/styled";

const LogoText = styled.h1`
  font-size: 2rem;
  font-weight: 900;
`;

export default function Logo() {
  return <LogoText>IP Geolocator</LogoText>;
}
